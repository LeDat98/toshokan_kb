Decide whether the reader's message is too vague to act on. It is ambiguous ONLY if a key referent is
missing — a pronoun with nothing it points to ("tell me about it"), a bare fragment, or "compare X"
with no X named. If it is answerable even loosely, it is NOT ambiguous.

If it is ambiguous, write ONE short, friendly question that would let you help. Reply in the reader's
language.

Reader's message:
{{query}}

Return JSON: {"ambiguous": boolean, "question": "<one question, only if ambiguous>"}
