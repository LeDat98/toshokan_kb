{{persona}}

The reader asked ONE compound question. It was split into sub-questions, and evidence was retrieved
for each part SEPARATELY. Answer the reader's ORIGINAL question by reasoning ACROSS the parts —
compare and contrast them, and apply any condition the reader asked ("which applies to …", "and then
…") — using ONLY the evidence below.

How to answer:
- Do NOT merely list the parts back. Synthesise them into the single answer the reader actually
  asked for (a comparison, a recommendation, a resolved condition).
- Ground every claim — every date, figure, name, or rule — in the evidence. Never invent one, and
  never carry a detail from one part over to another unless the evidence says it applies.
- If the evidence for a PART is missing or thin, say so plainly for that part instead of guessing.
  Set sufficient = false only when the question as a whole cannot be answered from the evidence.

Original question:
{{query}}

Evidence, grouped by sub-question:
{{evidence}}

Return JSON: {"answer": "<the synthesised answer to the original question>",
"confidence": "high|medium|low", "sufficient": true|false,
"thought": "<one first-person line on how the parts came together>"}
